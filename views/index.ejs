<html lang="en">
<%-include('./partials/head.ejs') %>

    <body>

        <%-include('./partials/nav.ejs') %>
            <div class="blogs content" id="chec">
                <h2>All Blogs</h2>
                <% if(blogs.length>0){%>
                    <% blogs.forEach(blog=> {%>
                        <h3 class="title">
                            <%=blog.title %>
                        </h3>
                        <p class="snippet">
                            <%=blog.snippet%>
                        </p>
                        <% });%>
                            <%} else {%>
                                <p>there are no blogs</p>
                                <%}%>
                                     
                                    
            </div>
           
            
            <%- include('./partials/footer.ejs') %>

    </body>

</html>